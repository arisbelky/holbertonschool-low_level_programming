#include <stdio.h>
#include <stdlib.h>
/**
 * main -start the program
 * Return: ends the execution
 */
int main(void)
{
	int n;

	n = 0;
	if (n < 10)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
