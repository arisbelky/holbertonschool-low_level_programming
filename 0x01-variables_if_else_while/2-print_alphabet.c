#include <stdlib.h>
#include <stdio.h>
/**
 * main -serves as the starting point for program execution
 * Return: ends the execution
 */
int main()
{
	char ch ='a';

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
		putchar('\n');
	return (0);
}
