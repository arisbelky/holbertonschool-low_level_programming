#include <stdlib.h>
#include <stdio.h>
/**
 * main -serves as the starting point for program execution
 * Return: ends the execution
 */
int main()
{
	char c = 'a';

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
		putchar('\n');
	return (0);
}
