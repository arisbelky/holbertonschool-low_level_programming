#include <stdio.h>
#include <stdlib.h>
/**
 * main -where the program begans
 * Return: ends the execution
 */
int main(void)
{
	int i;

	i = 0;
	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
